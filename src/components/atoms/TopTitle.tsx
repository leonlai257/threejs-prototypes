import { styled } from '@stitches/react';
import { flexCenter } from '../style/flex';

export const TopTitle = styled('div', {
    ...flexCenter,
    position: 'absolute',
    fontSize: '40px',
    top: '20px',
    width: '100%',
});
