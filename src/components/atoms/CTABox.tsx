import { styled } from '@stitches/react';
import { FlexBox } from './FlexBox';

export const CTABox = styled(FlexBox, {
    flexDirection: 'column',
    position: 'absolute',
    bottom: '60%',
    left: 0,
    right: 0,
    margin: 'auto',
});
